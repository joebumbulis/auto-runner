[Unit]
Description=CircleCI Runner
After=network.target
[Service]
ExecStart={{ RUNNER_DIR }}/circleci-launch-agent --config {{ RUNNER_DIR }}/launch-agent-config.yaml
Restart=always
User=root
NotifyAccess=exec
TimeoutStopSec={{ TIMEOUT_STOP_SEC }}
[Install]
WantedBy = multi-user.target